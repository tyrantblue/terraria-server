# 功能缺口与路线图

这份清单回答「还需要有什么功能」。按「对一个人维护的私服的实际价值」排序，
不谈远期幻想。✅ = 本轮已做。

状态：**2.0.0**（2026-09-19）。P1 四项（定时任务 / 备份恢复 / 日志时间戳+轮转 / 事件通知）已完成 ✅；
1.4.2 补上了「控制台心跳」；2.0.0 处理了 issue #1–#8：删掉旧 `/api/*`、配置不再回显明文密码、
WS 回放带真实 offset、守卫契约必填、世界元数据、审计落盘、上传上限、资源曲线。
下面保留原始评估与更新记录。

---

## 一、已经能用的（含本轮新增）

| 领域 | 能力 |
| --- | --- |
| 状态 | 服务器状态一次拿全、玩家列表（带 IP）、游戏时间/种子/MOTD ✅ |
| 控制 | 保存 / 结算液体 / 改时间 / 广播 / 踢人 / 封禁 / 结构化控制台 + WebSocket ✅ |
| 配置 | 读写 `serverconfig.txt`（持久化、原子写、保留注释），运行时可立即生效 ✅；**敏感键只回掩码** ✅ |
| 世界 | 列表 / 上传（413/507 预检 + 原子写）/ 删除 / 切换（后台任务）/ 一键备份 / 备份列表 ✅；**`.wld` 元数据（尺寸/难度/创建时间）** ✅ |
| 长任务 | 操作登记 + 进度查询 + 互斥，切换世界不再挂 30 秒 ✅ |
| 运维 | 连接守卫（限流/封禁/假满员自动恢复/自动白名单）、契约快照与版本握手 ✅ |
| 可观测 | 控制台心跳 + `log_stalled`/`log_age` + `log_stalled` 告警 ✅；CPU/内存/磁盘/在线人数曲线 ✅；控制台审计落盘 `control/audit.log` ✅ |

---

## 二、建议补的功能（按优先级）

### P1 运维刚需（✅ 本轮已完成）

1. **定时任务** ✅（已实现：`SCHEDULE_*` + `GET /api/v1/scheduler`）
   * 定时 `save`（比如每 15 分钟，防掉电丢档）；
   * 定时备份 + **自动清理旧备份**（保留最近 N 份 / N 天，现在备份只会越堆越多）；
   * 凌晨低峰定时重启（原版跑久了确实会僵）；
   * 实现成本低：服务里一个 `asyncio` 循环 + 一张任务表，配置写在 `docker-compose.yml` 的
     environment 里即可（和守卫一样的风格）。
2. **备份恢复** ✅（已实现：`POST /api/v1/backups/{name}/restore`，含 `auto:` 备份与安全副本）
   * 现在能创建/列出备份，但没有「从备份恢复」。
   * 恢复 = 停服 → 覆盖 `.wld` → 启服，天然适合做成操作（`world.restore` → 202）。
   * 顺带把 Terraria 自己生成的 `.wld.bak` / `.bak2` 也纳入统一视图。
3. **日志时间戳 + 轮转** ✅（已实现：`start.sh` 加 `[时间戳]`，`ops/logrotate.terraria` → `/etc/logrotate.d/terraria`）
   * `output.log` 目前**没有时间戳**（`tee -a` 直接落盘），面板只能显示相对顺序，
     排查问题时无法回答「几点被占满的」；
   * 文件无限增长（现在 500KB 级，长期会很大）。
   * 做法：`start.sh` 里给 `tee` 前加时间戳前缀 + 落一份 `/etc/logrotate.d/terraria`。
     注意：加前缀要同步改 `api/app/services/console/parser.py` 的正则与
     `classify_line`（日志格式变更属于接口行为变化，要走 CHANGELOG）。
4. **事件通知** ✅（已实现：飞书 / Discord / Slack / 通用 JSON / **QQ 频道机器人**，
   目标可在面板里改，见 `PUT /api/v1/notifications/settings`）
   * 玩家上线/下线、服务端崩溃、被扫描封禁、自动恢复（假满员）——这些现在只在
     `docker compose logs terraria-guard` 里，没人盯着就不知道。
   * 最省事：一个通用 webhook（Discord / Telegram / 企业微信），
     守卫和 API 各发几个事件即可。

### P2 好用但非必需

5. **白名单/封禁名单的可视化管理**
   * 守卫的白名单现在是 `guard/allow.txt`（手工编辑 + `apply`），面板看不到；
   * 学习型白名单在 `guard/learned_allow.txt`，也没有接口暴露；
   * 封禁名单只能读 `banlist.txt`，`unban` 是原版缺失的能力（已提供按行删除）。
   * 建议：加一组 `/api/v1/guard/*` 接口（读取/增删白名单、查看被封 IP 与剩余时间、
     手动解封），把两侧状态在面板里对齐。
6. **服务器资源与健康曲线** ✅（已实现：`GET /api/v1/metrics`，cgroup v2/v1，无非必需组件）
   * CPU/内存/磁盘、在线人数曲线、崩溃与自动恢复次数。
   * 容器指标可以直接读 cgroup，不需要额外组件；存最近 N 个采样点即可。
7. **操作审计落盘** ✅（已实现：`control/audit.log` + logrotate，`?tail=N` 可回读）
   * `POST /api/v1/console/commands` 已有内存审计（最近 200 条），重启即丢。
   * 落一份 `control/audit.log`（JSON Lines）成本很低，出问题能回溯。
8. **世界元信息** ✅（已实现：`services/world_header.py`，`GET /api/v1/worlds` 的 `metadata`）
   * 上传的 `.wld` 可以解析出尺寸（小/中/大）、难度、是否专家/大师、创建时间，
   * 面板上看着更直观。需要写一点 `.wld` 头部解析（格式公开，几十行）。
9. **上传的健壮性** ✅（已实现：`Content-Length` 中间件预检 + 流式上限 + 磁盘余量 + `.part` 原子写）
   * 目前没有大小上限、没有磁盘余量检查；
   * 建议加上限（比如 500MB）与 `Content-Length` 预检，避免把磁盘写满。
10. **守卫/通知阈值告警**（未做，可选）
    * `GET /api/v1/metrics` 已经有点了，但还没有「CPU/内存/人数超阈值 → webhook」；
    * 加一个阈值配置即可（复用 `Notifier`）。

### P3 想做再说

11. **鉴权**
    * 现状：只有来源 IP 白名单（`DOCKER-USER`），应用层无认证。
    * 单人使用够用；如果以后面板要给别人看，加一个静态 Bearer token 即可（十几行）。
    * 2.0.0 已经把「明文密码回显」这条最容易被打的洞堵上了（见 CHANGELOG），但
      `/api/v1/*` 的写接口仍然是无认证的——这是目前最大的剩余风险。
12. **TShock**
    * 需要权限组/领地/商店/反作弊才值得换；原版世界文件兼容，但改动面不小。
13. **多世界同时在线 / 多实例**
    * 当前是单实例单世界，架构上没必要改动。

---

## 三、刻意不做的

* **通用 SQL/ORM**：状态都在文件和控制台里，加数据库只会增加迁移负担。
* **用户体系/多租户**：一个人用，没有收益。
* **把守卫逻辑搬进 API 进程**：守卫需要宿主机 netfilter 权限，独立容器更干净
  （见 `connection-guard.md`）。
* **时序数据库**：资源曲线只保留内存里的 24 小时采样点；要长期存储再说，
  现在 Prometheus/Grafana 只会给单机私服增加维护面。

---

## 四、下一步顺序（2.0.0 之后）

1. **前端迁移到 `/api/v1`** ✅（面板 1.4.0 已迁完；旧接口在 2.0.0 删除）。
2. **守卫可视化** ✅（`/api/v1/guard/*`：白名单、学习型白名单、被封 IP 与剩余时间、手动解封）。
3. **资源与健康曲线** ✅（`/api/v1/metrics` + `log_stalled`/`log_age` + `log_stalled` 告警）。
4. **审计落盘** ✅（`control/audit.log`）。
5. **世界元信息** ✅（`.wld` 头部解析）。
6. **上传健壮性** ✅（大小上限、磁盘余量、原子写）。
7. **应用层鉴权**（P3 第 11 项）：目前 `/api/v1/*` 全部裸奔在 8080 上，只靠
   `DOCKER-USER` 的源 IP 白名单兜着；一个静态 token 就能显著降低风险。
8. **面板侧的密码输入法改造**：`GET /api/v1/config` 现在返回掩码，
   前端要改成「留空 = 不修改」（能力位 `config.password_masked` 可用来做灰度）。

---

## 五、已知风险

* **游戏容器的输出流水线是单点**。`start.sh` 里「服务端 → awk（打时间戳）→ tee → output.log」
  任何一环卡住，日志就停更；而 API 的命令回显依赖日志里的哨兵，
  所以表现是「游戏能连、面板读不到状态」。本轮上线时遇到过一次（约 6 分钟无输出，
  重启容器后恢复，未定位到确定原因）。
  **现在有主动检测**：`SCHEDULE_CONSOLE_CHECK_SECONDS` 心跳（哨兵无回显 → `log_stalled` 告警）
  \+ `GET /api/v1/server` 的 `log_stalled`/`log_age`（日志静默超过 `LOG_STALL_SECONDS`）。
  兜底手段仍是 `docker compose restart terraria`（会丢未保存进度，建议先 `save`）；
  如果反复出现，把 `start.sh` 里的 awk 那段去掉即可（解析器兼容无时间戳格式）。
* **操作状态只在 API 进程内存里**：重启后 `/api/v1/operations/{id}` 会 404。
* **资源曲线也只在内存里**：API 重启后 `/api/v1/metrics` 从零开始（这是有意的，见「刻意不做」）。
* **`banlist.txt` 的格式依赖原版实现**：只有在线玩家被封时才会写文件；
  `DELETE /api/v1/players/{name}/ban` 是按行改写，不保证与游戏内 `ban` 的所有细节一致。
