# Terraria world data
worlds/

# Backups
backup/

# Runtime files
control/

# Runtime/application data
data/

# Guard runtime state (auto-learned whitelist contains player IPs)
guard/learned_allow.txt
guard/learned_allow.txt.tmp

# 静态白名单里有真实玩家 IP（个人信息）：仓库里只留 allow.txt.example，
# 真实文件只放在服务器上 —— 迁移时需要单独 scp（见 README「迁移服务器」）。
guard/allow.txt

# Server secrets / local configuration
config/serverconfig.txt

# Local backup files
docker-compose.yml.bak

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Environment / secrets
.env
.env.*
!.env.example

# OS/editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
