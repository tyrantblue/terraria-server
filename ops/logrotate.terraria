# output.log 由容器里的 `tee -a` 持续追加，所以必须用 copytruncate：
# 截断原文件而不是改名重建，tee 的 fd 才继续有效（O_APPEND 会写到新末尾）。
# API 与守卫都按字节偏移读日志，截断后偏移变小 → 两边都会自动重置游标。
/opt/terraria/control/output.log {
    weekly
    rotate 4
    size 20M
    missingok
    notifempty
    copytruncate
    su root root
}

# 控制台命令审计（API 追加写，一行一条 JSON）。同样用 copytruncate：
# API 每次写入都重新 open(O_APPEND)，截断后会继续追加到新末尾，
# 只是「上周谁改了什么」这类历史会随轮转被清理（保留 12 份）。
/opt/terraria/control/audit.log {
    monthly
    rotate 12
    size 5M
    missingok
    notifempty
    copytruncate
    su root root
}
